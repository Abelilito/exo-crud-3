<div class="container">
  <div class="col-md-7">
    <h1> Review of <%= @restaurant.name %></h1>
    <br>
    <%= simple_form_for([@restaurant, @review]) do |f| %>
      <%= f.input :rating %>
      <%= f.input :content %>
      <%= f.submit "Submit review" %>
    <% end %>
  </div>
</div>
